import { ChartCard, DataTable, MetricGrid, PageHeading } from "@/components/management/dashboard-ui"; import { GlobalFilters } from "@/components/management/global-filters"; import { requireManagementAccess } from "@/features/management/access"; import { getManagementPageData } from "@/features/management/data";
export default async function CommercialPage(){await requireManagementAccess("analytics_commercial");const data=await getManagementPageData("commercial");return <><PageHeading eyebrow="Comercial" title="Venda com contexto" description="Pedidos e serviços, faturamento, funil e concentração"><GlobalFilters commercial/></PageHeading><MetricGrid metrics={data.metrics}/><div className="dashboard-grid"><ChartCard title="Funil comercial" subtitle="Quantidade e valor por etapa" data={data.chart} kind="bar"/><ChartCard title="Ranking de vendedores" subtitle="Top 10 por valor total" data={data.secondaryChart ?? []} kind="bar"/></div><div className="dashboard-grid lower"><DataTable title="Curva ABC de clientes" columns={["Cliente","Valor","% total","% acumulado","Classe"]} rows={data.rows}/><DataTable title="Pedidos e ordens de serviço recentes" columns={["Registro","Tipo","Cliente","Vendedor","Etapa","Valor","Situação"]} rows={data.secondaryRows ?? []}/></div></>}
