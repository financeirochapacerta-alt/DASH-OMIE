import { ChartCard, DataTable, MetricGrid, PageHeading } from "@/components/management/dashboard-ui"; import { GlobalFilters } from "@/components/management/global-filters"; import { requireManagementAccess } from "@/features/management/access"; import { getManagementPageData } from "@/features/management/data";
export default async function FinancialPage(){await requireManagementAccess("financial");const data=await getManagementPageData("financial");return <><PageHeading eyebrow="Financeiro" title="Compromissos sob controle" description="Receber, pagar, vencimentos e inadimplência gerencial"><GlobalFilters /></PageHeading><MetricGrid metrics={data.metrics}/><ChartCard title="Entradas x saídas realizadas" subtitle="Movimentos quitados, últimos 6 meses" data={data.chart} kind="bar"/><DataTable title="Movimentos financeiros (não cancelados)" columns={["Contraparte","Categoria","Tipo","Documento","Vencimento","Valor","Status"]} rows={data.rows}/></>}
